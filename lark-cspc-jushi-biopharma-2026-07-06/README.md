# GitHub Pages deployment

This folder is the GitHub Pages-ready static version of the Jushi Biopharma Feishu deck.

Deploy the folder root as the GitHub Pages root. The entry file is `index.html`; keep `assets/`, `input/`, and `viewer-style-1.css` next to it.
